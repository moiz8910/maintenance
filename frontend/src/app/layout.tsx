import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "AI-Powered Maintenance Intelligence Platform",
  description: "Enterprise dashboard + AI agent system for maintenance managers",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.className} bg-slate-50 text-slate-950 antialiased h-screen w-screen overflow-hidden`}>
        {children}
      </body>
    </html>
  );
}
